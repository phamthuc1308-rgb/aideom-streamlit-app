# AIDEOM-VN Premium V4

Chạy local:

```bash
pip install -r requirements.txt
streamlit run app.py
```

Bản V4: giao diện sáng, bỏ bảng đen ở trang chủ, Bài 4 có hard/soft fairness fallback để không lỗi khi ràng buộc C5 gốc không khả thi.
